apiVersion: v1
kind: Service
metadata:
  name: {{ include "wellenplan.fullname" . }}
  labels:
    {{- include "wellenplan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wellenplan.selectorLabels" . | nindent 4 }}
