apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wellenplan.fullname" . }}-test-connection"
  labels:
    {{- include "wellenplan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wellenplan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
